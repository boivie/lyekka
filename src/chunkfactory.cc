#include "chunkfactory.h"

using namespace Lyekka;
using namespace std;

Chunk ChunkFactory::create(boost::filesystem::path& path, uint64_t offset, size_t size, ChunkBuffer& buffer)
{
  Chunk chunk;
  return chunk;
}
